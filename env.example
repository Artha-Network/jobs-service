# Jobs Service environment example
# Copy to `.env` and fill real values. Comments show purpose and expected format.

# -------------------------------------------------------------------
# Core infrastructure
# -------------------------------------------------------------------

# Redis connection string for BullMQ (required)
# Examples:
#   redis://localhost:6379
#   rediss://:<password>@my-redis.example.com:6380
REDIS_URL=redis://localhost:6379

# Solana RPC endpoint used by infra/chain.client.ts (optional in this scaffold)
# Example: https://api.devnet.solana.com  or a provider URL
RPC_URL=

# Base URL for Actions or API server that can prepare ready-to-sign txs
# Example: https://actions.artha.example.com/
ACTIONS_BASEURL=

# Shared secret to verify Helius webhook signatures (required for webhook intake)
# This is the raw secret string you configure in the Helius dashboard.
HELIUS_WEBHOOK_SECRET=

# -------------------------------------------------------------------
# Policy flags (ChainPort). Default is conservative: no auto-finalize.
# Set to true/false, yes/no, 1/0, on/off (case-insensitive).
# -------------------------------------------------------------------

# Allow preparing RELEASE finalize flows automatically (still needs human approval)
AUTO_FINALIZE_RELEASE=false

# Allow preparing REFUND finalize flows automatically (still needs human approval)
AUTO_FINALIZE_REFUND=false

# -------------------------------------------------------------------
# Notifications
# -------------------------------------------------------------------

# Notification driver: "noop" (logs only) or "dialect" (external provider)
NOTIFY_DRIVER=noop

# API key or token for the Dialect driver (only if NOTIFY_DRIVER=dialect)
NOTIFY_DIALECT_KEY=

# -------------------------------------------------------------------
# Worker runtime
# -------------------------------------------------------------------

# Concurrency level for BullMQ workers (integer). Default 5.
WORKER_CONCURRENCY=5

# Log level: debug | info | warn | error. Default info.
LOG_LEVEL=info

# Timezone for human-readable timestamps in logs (does not affect Unix seconds)
# Example: America/Chicago
TZ=America/Chicago
